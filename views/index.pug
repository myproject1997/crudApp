extends layout
block content
    br
    h1 #{title}
    br
    ul.list-group
        each article, i in articles
            li.list-group-item 
                a(href="/articles/" + article.id) #{article.title}

    br